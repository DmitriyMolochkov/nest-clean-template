// Redis prefixes for cache keys
export const USER_REFRESH_TOKEN_KEY_PREFIX = 'user_refresh_token';

// Dependency tokens
export const JWT_CONFIG = 'jwt_config';
export const BULL_CONFIG = 'bull_config';
